export interface Subscription {
    month : number;
    totalPrice : number,
    perMonthPrice: number,
    activePlan: boolean,
    selectedPlan: boolean
}
export interface HeroText {
    slashedPrice: number,
    currentPrice : number
}


